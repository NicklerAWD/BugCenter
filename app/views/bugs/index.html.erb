<% if signed_in? %>
	<% provide(:title, 'All bugs') %>
	<h1>All bugs</h1>

	<%= will_paginate %>

	<ul class="bugs">
	  <%= render @bugs %>
	</ul>

	<%= will_paginate %>
<% else %>
	<% provide(:title, "403" ) %>
	<h2>Sorry, you don't have the rights. Please contact the adminstrators.</h2>
<% end %>